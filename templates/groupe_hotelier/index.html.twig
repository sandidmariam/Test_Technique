{% extends 'base.html.twig' %}

{% block title %}GroupeHotelier {% endblock %}

{% block body %}
    <br>
    <div>
    <h1>Groupe Hotelier</h1>
        <br>

    <table class="table" style="margin-left: 20px;margin-right: 30px;">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for groupe_hotelier in groupe_hoteliers %}
            <tr>
                <td>{{ groupe_hotelier.id }}</td>
                <td>{{ groupe_hotelier.nom }}</td>
                <td>{{ groupe_hotelier.Description }}</td>
                <td>
                    <a href="{{ path('app_groupe_hotelier_show', {'id': groupe_hotelier.id}) }}"><img src="{{ asset('/assets/show.png') }}" style="width: 35px;height: 35px"></a>
                    <a href="{{ path('app_groupe_hotelier_edit', {'id': groupe_hotelier.id}) }}"><img src="{{ asset('/assets/edit.png') }}" style="width: 35px;height: 35px"></a>
               {{ include('groupe_hotelier/_delete_form.html.twig') }}

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        <a href="{{ path('app_groupe_hotelier_new') }}"><img src="{{ asset('/assets/add.png') }}" style="width: 35px;height: 35px"></a>

        </tbody>
    </table>


    </div>
{% endblock %}
